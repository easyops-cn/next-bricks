export const ALLOWED_BUTTON_TYPES = [
  "primary",
  "default",
  "dashed",
  "ghost",
  "link",
  "text",
  "icon",
  "neutral",
] as const;

export const ALLOWED_COMPONENT_SIZES = [
  "large",
  "medium",
  "small",
  "xs",
] as const;

export const ALLOWED_SHAPES = ["round", "circle"] as const;
